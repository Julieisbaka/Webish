{
  "name": "webish",
  "version": "0.0.1",
  "private": false,
  "dependencies": {
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "react-scripts": "5.0.1",
    "@octokit/rest": "^20.0.2"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "@types/react": "^18.2.55",
    "@types/react-dom": "^19.1.2"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "postinstall": "npm i --package-lock-only",
    "create-lock": "npm i --package-lock-only",
    "setup": "npm install && npm run create-lock"
  },
  "homepage": "https://julieisbaka.github.io/Webish",
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}